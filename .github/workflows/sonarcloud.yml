name: SonarCloud Analysis

on:
  push:
    branches:
      - main

  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  sonarcloud:
    name: SonarCloud Scan
    runs-on: ubuntu-latest

    permissions:
      pull-requests: read
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            server/package-lock.json
            app/package-lock.json

      - name: Install server dependencies
        working-directory: server
        run: npm ci

      - name: Install app dependencies
        working-directory: app
        run: npm ci --legacy-peer-deps

      - name: Wait for PostgreSQL to be ready
        run: |
          until pg_isready -h localhost -p 5433; do
            echo "Esperando a que la base de datos esté lista..."
            sleep 2
          done

      - name: Push Prisma Schema to Test DB
        working-directory: server
        env:
          DATABASE_URL: postgresql://test_user:test_password@localhost:5433/lifetime_test_db?schema=public
        run: npx prisma db push

      - name: Run server tests with coverage
        working-directory: server
        env:
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          DATABASE_URL: postgresql://test_user:test_password@localhost:5433/lifetime_test_db?schema=public
        run: npm run test:ci

      - name: Run app tests with coverage
        working-directory: app
        run: npm run test:ci

      - name: Fix LCOV paths for SonarCloud
        run: |
          sed -i 's|^SF:|SF:server/|' server/coverage/lcov.info
          sed -i 's|^SF:|SF:app/|' app/coverage/lcov.info

      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@2f77a1ec69fb1d595b06f35ab27e97605bdef703
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}